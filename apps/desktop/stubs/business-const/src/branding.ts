/**
 * Mirrors the real package's `./branding` subpath — `packages/model-bank`
 * imports `@lobechat/business-const/branding` directly, and without this the
 * subpath is unresolvable for anything that goes through this stub.
 */
export * from '../../../../../packages/business/const/src/branding';
